package com.nutomic.syncthingandroid.util;

/**
 * Adds a listener for the back button click.
 * Useful for handling back button events in a PreferenceFragment.
 */
public interface OnBackClickListener {
    boolean onBackClick();
}
